# Supabase — à renseigner une fois le projet Supabase créé (T-003).
# Copier ce fichier en `.env.local` (non commité) et remplir les valeurs.
# Sur Vercel : définir ces variables dans Project Settings → Environment Variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
