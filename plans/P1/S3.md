# P1 · S3 — Écran Annuaire (recherche, filtres, mes/tous, sélection)   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 2 · Dépend de : S1, S2, S7 (faites et commitées)**
> Exécutant : UNIQUEMENT les tâches ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Design fixé (maquette) — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.
> Ne pas `git commit` / `git push`. Pas de validation navigateur (elle est humaine).

- Date : 2026-07-16 · Branche : —

## Lire (commun à la session)

- `ARCHITECTURE.md` §Écran 2 (Annuaire) + §Maquette (design system + écarts).
- `design/maquettes/design-annuaire-msp/project/MSP Annuaire.dc.html` **lignes ~64-162** (bloc ANNUAIRE)
  — rendu exact à reproduire (valeurs des styles inline). Les 2 PNG de `project/uploads/` montrent le
  rendu attendu d'une ligne et du popover.
- `DECISIONS.md` §Recherche et filtres côté client · §propriété/adoption · §commentaires 4 types.
- Code existant (à **réutiliser, pas réécrire**) : `src/data/DirectoryProvider.tsx` (`useDirectory()`),
  `src/data/search.ts` (`filterContacts`), `src/types/db.ts` (`ContactWithMeta`, `COMMENT_TYPES`),
  `src/components/` (`Badge`, `CommentIcons` variant `compact`, `Avatar`, `StarToggle`),
  `src/app/Layout.tsx`, `src/features/auth/AuthProvider.tsx`.

## Hors périmètre

- Les écrans fiche / édition / impression restent des stubs (S4, S5, S6). Cliquer une ligne **navigue**
  vers `/contact/:id` (le stub s'affichera) — c'est normal.
- Ne pas modifier `src/components/`, `src/theme/`, `src/data/`. Aucune nouvelle dépendance.

---

## T6 — Écran Annuaire + état de sélection partagé

### Objectif
L'écran central : retrouver un contact en quelques secondes, basculer mes/tous, et cocher des contacts
pour l'impression.

### Décision clé
**Sélection d'impression = état client transitoire partagé** (cf. `ARCHITECTURE.md` §Découpage) : il est
lu par l'annuaire (cases à cocher), par la barre du haut (« N sélectionné(s) → Imprimer ») et plus tard
par l'écran impression (S6). → créer `src/app/SelectionProvider.tsx` (contexte : `selectedIds: Set<string>`,
`toggle(id)`, `remove(id)`, `clear()`, `count`), monté dans `src/main.tsx` **sous** `AuthProvider`, et
consommé par `Layout` pour l'indicateur. Ne rien persister (transitoire).
**Recherche/filtres 100 % côté client** via `filterContacts` — ne pas requêter Supabase ici.

### Lire / Modifier
- Créer `src/app/SelectionProvider.tsx` (+ hook `useSelection()`).
- Modifier `src/main.tsx` (monter `<SelectionProvider>`), `src/app/Layout.tsx` (indicateur
  « {count} sélectionné(s) → Imprimer », visible seulement si `count > 0`, cliquable → `/impression`).
- Remplacer le stub `src/features/annuaire/AnnuairePage.tsx` (peut être découpé en sous-composants
  locaux au dossier, ex. `ContactRow.tsx`, `FiltersBar.tsx`).

### Étapes
1. **Barre de recherche** (maquette l.68-71) : champ réel, placeholder
   « Rechercher un nom, une spécialité, un tag, un commentaire… », icône loupe SVG.
2. **Bascule « Mes contacts / Tous »** (l.72-75) : segmented control, styles actif/inactif de la maquette.
   « Mes contacts » = `contact.isMine` (créé par moi **ou** adopté).
3. **Filtres** (l.77-85) : rangée de chips. Booléens à bascule : **Secteur 1**, **VAD**, **AME/CMU**,
   **+ Nouveaux patients** (`prend_nouveaux === 'oui'`). Plus, au **même style de chip**, trois
   sélecteurs : **Arrondissement**, **Profession/spécialité**, **Tag** (valeurs dérivées des contacts
   chargés, pas en dur) — cf. `ARCHITECTURE.md` §Maquette écart 5. Chip actif = couleur pleine.
4. **Compteur + tri** (l.84) : « {n} résultats · Tri ▾ » → **Pertinence** (défaut, ordre de
   `filterContacts`) · **Nom (A→Z)** · **Arrondissement**.
5. **Lignes** (l.87-160) : carte blanche radius 14, et dans l'ordre — case à cocher (sélection
   impression) · `Avatar` · bloc cliquable `nom` + `{profession} · {arrondissement} arr.` (→
   `/contact/:id`) · `Badge` conditionnels (Secteur 1 / Secteur 2 / AME/CMU / VAD) · `CommentIcons`
   **variant `compact`** (uniquement les types présents) · **téléphone patient** (`tel_secretariat`) en
   lien `tel:` · `StarToggle` (adopter/retirer → `adoptContact`/`unadoptContact` du provider).
6. États : `loading` (skeleton ou texte sobre), erreur, et **liste vide** — message utile
   (ex. « Aucun contact pour l'instant — commencez par en ajouter un » avec lien vers `/nouveau`),
   car la base est vide au départ.

### Validation
- Auto (bloque le commit) : `npm run build` + `npm run typecheck` → 0 erreur.
- Humain (visuel) : consigner la checklist dans ce fichier (§Bilan) — rendu des lignes, popovers,
  filtres, mes/tous, sélection, responsive mobile.

### Si bloqué
`filterContacts` ne couvre pas un filtre voulu → **ne pas modifier `src/data/search.ts`** (zone S2) :
filtrer en complément dans l'écran, et le signaler dans le bilan.

### Message de commit (fin de plan)
`feat(annuaire): directory screen with search, filters, selection`

### Statut
[x] fait · exécuté le : 2026-07-16 · commit : —

---

## Bilan de session

### Fichiers créés
- `src/app/SelectionProvider.tsx` — contexte sélection d'impression (`selectedIds`, `toggle`, `remove`,
  `clear`, `count`) + hook `useSelection()`, même forme que `DirectoryProvider`/`AuthProvider`.
- `src/features/annuaire/FiltersBar.tsx` — barre de recherche + bascule Mes contacts/Tous + rangée de
  filtres + compteur/tri (maquette l.67-85).
- `src/features/annuaire/ContactRow.tsx` — une ligne d'annuaire (maquette l.89-158, cf. les 2 PNG).
- `src/features/annuaire/sort.ts` — `sortContacts()` (pertinence/nom/arrondissement), fonction pure
  isolée à côté de `search.ts` (non modifié).

### Fichiers modifiés
- `src/main.tsx` — monte `<SelectionProvider>` sous `<AuthProvider>` (au-dessus de `DirectoryProvider`).
- `src/app/Layout.tsx` — l'indicateur de sélection lit `useSelection()` au lieu d'une prop
  `selectedCount` (qui n'était alimentée par personne) ; `Layout` ne prend plus de props.
- `src/features/annuaire/AnnuairePage.tsx` — écran complet (état recherche/filtres/tri, états
  loading/erreur/vide, liste des lignes).

### État de sélection partagé (`SelectionProvider`)
Contexte React monté dans `main.tsx` entre `AuthProvider` et `DirectoryProvider` (transitoire, aucune
persistance — un rechargement de page vide la sélection). `AnnuairePage`/`ContactRow` appellent
`toggle(id)` depuis la case à cocher de chaque ligne ; `selectedIds.has(id)` pilote l'état coché.
`Layout` lit `count` et affiche l'indicateur « {count} sélectionné(s) → Imprimer » (lien vers
`/impression`) uniquement si `count > 0` — remplace l'ancienne prop `selectedCount` non câblée. L'écran
impression (S6) pourra consommer `selectedIds`/`remove`/`clear` pour le panneau réordonnable.

### Décisions d'implémentation (non explicitement fixées par la maquette)
- **Mes contacts par défaut** : la maquette a pour état initial `mesContacts: true` (ligne 474 du
  `.dc.html`) → repris tel quel, la bascule ouvre donc sur « Mes contacts ».
- **3 sélecteurs Arrondissement/Profession/Tag** (écart maquette #5, absents des bindings réels du
  prototype — juste du texte statique) : stylés comme les chips muets (fond `#f1ede4`), texte plus
  foncé (`colors.text.primary`, 600) une fois une valeur choisie faute de couleur sémantique dédiée
  dans la maquette pour cet état. Options dérivées des `contacts` chargés (pas en dur), triées FR.
- **Tri** : la maquette n'affiche qu'un texte statique « Pertinence ▾ » (aucun binding réel) → remplacé
  par un vrai `<select>` stylé sobrement dans le même bloc texte.
- **États loading/erreur** : pas de rouge « alerte » dans le design system extrait de la maquette (ton
  volontairement non agressif, cf. `ARCHITECTURE.md` §Ton visuel) → texte sobre en couleurs neutres
  existantes, pas de couleur inventée.
- **Placeholder de la recherche** (`#9a9488`) : posé via un `<style>` scopé au composant
  `FiltersBar.tsx` (`::placeholder` n'est pas exprimable en style inline React) — n'a pas touché
  `src/theme/`.

### Écarts avec S3.md
Aucun écart de périmètre ni de zone modifiée. Seul ajustement au-delà du texte de l'étape 6 : l'état
« liste vide après filtrage » (base non vide mais 0 résultat) propose en plus un bouton « Voir tous les
contacts » quand la cause est la bascule « Mes contacts » (sinon aucune action ne permettait de sortir
de cet état) — cohérent avec l'esprit de l'étape 6 (« message utile »), pas prescrit littéralement.

### Validation automatique
`npm run build` → 0 erreur · `npm run typecheck` → 0 erreur (confirmé après l'implémentation complète).

### Checklist visuelle (humain — pas de navigateur côté agent)
> La base étant vide au démarrage, prévoir quelques contacts de test (avec commentaires des 4 types,
> `secteur_conv`, `vad`, `ame_cmu`, `tags`, `tel_secretariat`) pour juger les points ci-dessous.
- [ ] Rendu d'une ligne conforme aux 2 PNG de référence (`project/uploads/pasted-...png`) : avatar,
      nom/profession/arrondissement, badges, icônes de commentaires, téléphone, étoile.
- [ ] Popover au survol (desktop) d'une icône de commentaire ; reste ouvert/se ferme correctement.
- [ ] Popover au tap (mobile/tactile) d'une icône de commentaire.
- [ ] Chips Secteur 1 / VAD / AME-CMU / + Nouveaux patients : couleur pleine à l'activation, style muet
      sinon.
- [ ] Sélecteurs Arrondissement / Profession / Tag filtrent correctement, options cohérentes avec les
      contacts chargés.
- [ ] Bascule Mes contacts / Tous : style actif (fond blanc + ombre) vs inactif conforme.
- [ ] Compteur de résultats + tri (Pertinence / Nom A→Z / Arrondissement) fonctionnent.
- [ ] Case à cocher d'une ligne → indicateur « N sélectionné(s) → Imprimer » dans la barre du haut,
      cliquable vers `/impression`.
- [ ] Clic sur une ligne (avatar ou bloc nom) → navigue vers `/contact/:id` (stub S4).
- [ ] Étoile (StarToggle) → adopte/retire de « Mes contacts » (persiste après rechargement).
- [ ] État base vide : message + bouton « + Ajouter un contact » vers `/nouveau`.
- [ ] État recherche/filtres sans résultat : message + bouton(s) de sortie (réinitialiser / voir tous).
- [ ] Responsive mobile : recherche/toggle/chips passent en colonne proprement (flex-wrap) ; ligne de
      contact reste lisible (léger débordement horizontal possible, non redessiné — cf. maquette qui ne
      prévoit pas de wrap sur la ligne elle-même).

---

## Fin de session
Consigner le bilan + la checklist visuelle **dans ce fichier** (§Bilan de session) ; ne pas toucher
`STATUS.md`/`TASKS.md`/`index.md`/`VALIDATION.md` ; aucun commit.
