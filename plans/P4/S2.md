# P4 · S2 — L'extracteur Doctolib + le bookmarklet + notice d'installation   (rédigé par Opus)

> **Modèle : Sonnet/Opus · effort : high · Vague : 2 · Dépend de : S1 (contrat `prefill`) + app déployée**
> Exécutant : UNIQUEMENT les tâches ci-dessous. Doute ou blocage → STOP, signale, rends la main.
> Ne pas `git commit` / `git push`. **Vérification réelle = humaine** (Thibault, sur une page Doctolib).

- Date : — · Branche : —

## Lire (commun à la session)

- `plans/P4/index.md` §Le contrat `prefill` + §Décisions (JSON-LD d'abord, DOM en repli, relais par URL,
  patient-only, CSP).
- `plans/P4/S1.md` — le **contrat exact** produit : clés autorisées, base64url, route `/nouveau?prefill=`.
- `DECISIONS.md` 2026-07-17 (Claude in Chrome / injection de prompt) et §T-005 (403 Doctolib) — le
  **pourquoi** de cette approche par le navigateur du membre.

## Hors périmètre

- **Ne touche pas au code de l'app React** (S1 a fait la partie in-app). Tout vit dans
  `tools/doctolib-bookmarklet/`.
- Pas d'extension navigateur ici (ce serait S3, conditionnelle).
- Ne pas contourner l'anti-bot de Doctolib, ne pas faire de requête réseau vers Doctolib : on lit
  **seulement** la page **déjà chargée** dans le navigateur du membre.

---

## T1 — L'extracteur (JSON-LD prioritaire, DOM en repli)

### Objectif
Depuis une page Doctolib ouverte, produire un objet conforme au **contrat `prefill`** de S1.

### Décision clé
- **Priorité JSON-LD** : lire les `<script type="application/ld+json">` de la page, parser le/les objets
  schema.org (`Physician`, `MedicalBusiness`, `MedicalClinic`, `Person`) et mapper :
  `name → nom`/`prenom`/`civilite` (découpage prudent), `medicalSpecialty`/`jobTitle → profession`,
  `address (PostalAddress) → adresse` + `arrondissement` (dérivé du code postal `75020` → `20e`),
  `geo (latitude/longitude) → latitude/longitude` (**+ `geocode_score` élevé** puisque source directe),
  l'URL canonique → `doctolib`, `sameAs`/`url` de cabinet → `site_web`, `knowsLanguage → langues`.
- **Repli DOM** (heuristique) uniquement pour les champs qu'aucun JSON-LD ne donne — sélecteurs ciblés,
  **tolérants à l'absence** (jamais d'exception si un nœud manque).
- **Patient-only** : ne mapper **que** des clés de la liste blanche S1. **Ne jamais** produire un champ
  pro (Doctolib n'en expose pas, et le lecteur S1 les rejetterait de toute façon — double barrière).
- `source_url` = l'URL de la page courante.

### Étapes
1. Créer `tools/doctolib-bookmarklet/extract.js` — **source lisible et commentée** (c'est elle qu'on
   maintiendra). Fonction `extractFromPage(document, location): Prefill`.
2. Encoder le résultat en **base64url** et construire `${APP_ORIGIN}/nouveau?prefill=<data>` ;
   `window.open(url, '_blank')`. `APP_ORIGIN` = l'URL Vercel de prod (constante en tête de fichier,
   **à renseigner**).
3. Champs manquants → simplement absents du payload (le formulaire les laisse vides). **Ne rien
   inventer** (doctrine T-005).

## T2 — Le bookmarklet + la notice

### Étapes
1. Produire le **bookmarklet** : la version minifiée `javascript:(()=>{…})()` de `extract.js`, dans
   `tools/doctolib-bookmarklet/bookmarklet.txt` (+ un petit script/commande de build documenté, ou une
   minification manuelle tracée — pas de chaîne de build lourde pour ~40 lignes).
2. **Notice d'installation** `tools/doctolib-bookmarklet/README.md` pour les ~10 membres :
   - glisser le lien dans la barre de favoris (30 s), captures/étapes ;
   - usage : ouvrir la fiche Doctolib du praticien → cliquer le favori → relire et enregistrer ;
   - rappel : **on relit toujours** (source déclarative, fiche « à vérifier »).
3. **Test CSP réel (Thibault)** : vérifier sur une vraie page Doctolib que le bookmarklet **s'exécute**.
   - S'il marche : consigner OK, S3 n'est pas déclenchée.
   - **S'il est bloqué par la CSP** de Doctolib : le **constater dans le bilan** → déclenche **S3**
     (mini-extension, même logique de lecture, non soumise à la CSP de la page). C'est le seul point qui
     décide de S3.

### Validation
- Auto : la logique de mapping de `extract.js` est **testable hors navigateur** (passer un
  `document`/HTML de fixture — un extrait de page Doctolib JSON-LD réel anonymisé). Si vitest en place,
  tester : JSON-LD complet → payload attendu ; JSON-LD partiel + repli DOM ; **aucun champ pro produit** ;
  page sans JSON-LD → payload minimal sans exception.
- Humain (réel) : Thibault teste sur ≥ 2 pages Doctolib (un praticien, un centre) → l'annuaire s'ouvre
  pré-rempli, données cohérentes, CSP OK/KO consigné.

### Si bloqué
JSON-LD absent ou structure inattendue sur les pages réelles → s'appuyer davantage sur le repli DOM
**pour les seuls champs sûrs**, et **documenter** ce que Doctolib expose réellement (ça oriente S3 et la
maintenance). Ne pas deviner un mapping incertain.

### Message de commit (fin de plan)
`feat(tools): Doctolib bookmarklet — extract page to annuaire prefill`

### Statut
[x] fait (T1 + T2 étapes 1-2 ; **T2 étape 3 — test CSP réel — non exécutée**, cf. Bilan)
· exécuté le : 2026-07-18 · commit : —

---

## Bilan de session

### Ce qui a été fait

- **T1 — `tools/doctolib-bookmarklet/extract.js`** : extracteur complet, JSON-LD prioritaire + repli
  DOM, conforme au contrat `Prefill` figé par S1 (`src/features/edition/formState.ts` — 13 clés
  texte/enum + `source_url` + `latitude`/`longitude`/`geocode_score`). `APP_ORIGIN =
  'https://annuaire-msp.vercel.app'` en constante en tête de fichier, exactement la valeur fournie
  (non devinée).
- **T2 étapes 1-2** : bookmarklet minifié (`bookmarklet.txt`, généré via `build.cjs` + `esbuild`,
  déjà présent dans `node_modules` comme dépendance de Vite — aucune nouvelle dépendance, aucun
  `package.json` touché) et notice d'installation (`README.md`) pour les ~10 membres.
- **T2 étape 3 (test CSP réel sur une vraie page Doctolib) : NON EXÉCUTÉE — volontairement.** Un
  agent n'a pas de navigateur pour ouvrir une vraie page Doctolib et vérifier la CSP ; c'est une
  vérification humaine explicitement confiée à Thibault (cf. tâche reçue). Le README documente la
  marche à suivre pour ce test (§Vérification avant diffusion) et où consigner le verdict.

### Structure JSON-LD réelle observée

**Aucune** — pas d'accès à une vraie page Doctolib depuis cette session (agent sans navigateur).
Le mapping d'`extract.js` s'appuie sur le vocabulaire schema.org **standard** documenté dans
`plans/P4/index.md` §Décisions (`Physician`/`MedicalBusiness`/`MedicalClinic`/`Person`,
`PostalAddress`, `GeoCoordinates`, `medicalSpecialty`/`jobTitle`, `knowsLanguage`, `sameAs`) — pas
une donnée inventée pour Doctolib spécifiquement, mais pas non plus vérifiée sur le terrain. Le
repli DOM (h1, liens `tel:`/`mailto:`, `[itemprop="address"]`) est délibérément minimal et prudent
pour la même raison : sans page réelle sous les yeux, deviner des sélecteurs plus fins aurait été
la doctrine T-005 (« ne jamais inventer ») appliquée à l'envers. **Ce point est explicitement à
réviser après le premier test réel** (cf. §Si bloqué du plan, et §Verdict CSP ci-dessous).

### Champs réellement récupérables

Inconnu à ce stade (dépend de la structure réelle, non observée — voir ci-dessus). Le code est
conçu pour dégrader proprement quel que soit ce qui manque : chaque champ absent du JSON-LD/DOM
reste simplement absent du payload (jamais d'exception, jamais de valeur inventée), et
`pickAllowedKeys` garantit qu'aucune clé hors contrat ne peut s'y glisser (double barrière avec la
liste blanche du lecteur côté app).

### Verdict CSP

**Verdict CSP : non testé (étape humaine restante, à faire par Thibault sur une vraie page
Doctolib) → P4/S3 reste non déclenchée pour l'instant, décision à prendre après ce test.**

### `APP_ORIGIN` utilisée

`https://annuaire-msp.vercel.app` — valeur fournie explicitement pour cette session (domaine Vercel
stable de prod), non devinée. Constante en tête d'`extract.js`, reprise automatiquement dans le
bookmarklet minifié.

### Fichiers créés

- `tools/doctolib-bookmarklet/extract.js` — source lisible et commentée (extracteur).
- `tools/doctolib-bookmarklet/build.cjs` — script de minification (esbuild → IIFE →
  `bookmarklet.txt`), autonome, sans dépendance ajoutée. Extension `.cjs` volontaire : le
  `package.json` de l'app déclare `"type": "module"`, ce script utilise `require()`.
- `tools/doctolib-bookmarklet/bookmarklet.txt` — bookmarklet minifié généré (`javascript:...`,
  ~4,8 Ko), régénérable via `node tools/doctolib-bookmarklet/build.cjs`.
- `tools/doctolib-bookmarklet/README.md` — notice d'installation (Chrome/Edge, Firefox, Safari) +
  usage + rappel prudence + marche à suivre pour le test CSP + note de maintenance.

### Écarts avec le plan

- **T2 étape 3 non exécutée** (voir ci-dessus) — conformément à la consigne reçue pour cette
  session : c'est un test humain, pas une tâche d'agent.
- `secteur_conv` : **non extrait** par `extract.js`, alors que la clé fait partie du contrat
  `Prefill`. Aucun signal JSON-LD schema.org standard ne porte le secteur conventionné, et le
  deviner depuis un texte libre du DOM (sans page réelle pour valider un sélecteur) aurait violé la
  doctrine « ne jamais inventer » (DECISIONS.md T-005). Documenté en commentaire dans `extract.js`
  (§4 Assemblage). Le membre le renseigne à la main si besoin, comme en saisie manuelle.
- `geocode_score` : fixé à `1` (valeur maximale) quand des coordonnées GPS sont lues directement du
  JSON-LD — décision de conception documentée dans le code (`mapGeo`), cohérente avec l'échelle du
  géocodage BAN de P3 (score 0..1, seuil 0,6) et avec la décision clé du plan (« geocode_score élevé
  puisque source directe »). Ce n'est pas une donnée inventée : c'est un choix d'échelle explicite,
  pas une mesure.
- Validation automatique (vitest) : **non ajoutée**, comme en S1 — vitest n'est pas configuré dans
  ce projet (aucun script de test, aucune dépendance de test dans `package.json`). La logique de
  mapping reste testable hors navigateur en théorie (fonctions pures `extractFromPage`,
  `mapAddress`, `mapGeo`, etc. prenant `document`/`location` en paramètres), mais aucun harnais de
  test n'existe pour l'exécuter — signalé plutôt qu'ajouter une dépendance hors périmètre.
- Aucune modification de code React (`src/features/edition/`, etc.) — hors périmètre respecté.
  Seule collision constatée avec du travail concurrent : `package.json`/`package-lock.json` et
  plusieurs fichiers `src/` apparaissent modifiés par d'autres sessions en cours (vague parallèle,
  P3 notamment) — non touchés par cette session, `git status` le confirme (tout ce qui est propre à
  S2 est sous `tools/`, non suivi jusqu'ici).

### Validation automatique

- `npm run build` → succès (0 erreur `tsc`, bundle Vite généré ; `tools/` n'est pas inclus dans le
  bundle de l'app, sans rapport avec ce gate).
- `npm run typecheck` → 0 erreur (`tools/` est hors du périmètre `include` de `tsconfig.json` — ne
  concerne que `src` et `vite.config.ts` — donc pas de vérification TS sur `extract.js`/`build.cjs`,
  cohérent avec le choix « pas de chaîne de build lourde » du plan).

### Checklist (humain — sur de vraies pages Doctolib)
- [ ] Bookmarklet installé (favori) ; clic sur une page praticien → nouvel onglet `/nouveau` pré-rempli.
- [ ] Champs cohérents : nom, spécialité, adresse, lien Doctolib ; **aucun champ pro** rempli.
- [ ] Page « centre / structure » : dégradation propre (ce qui est là est rempli, le reste vide).
- [ ] Page sans JSON-LD exploitable : pas de plantage, payload minimal.
- [ ] **Verdict CSP consigné** : le bookmarklet s'exécute (OK) ou est bloqué (→ déclenche S3).
- [ ] Fiche enregistrée depuis un préremplissage → « À vérifier » + provenance Doctolib (chaîne S1).

## Fin de session
Consigner le bilan + le **verdict CSP** **dans ce fichier** ; ne pas toucher
`STATUS.md`/`TASKS.md`/`index.md`/`VALIDATION.md` ; aucun commit.
